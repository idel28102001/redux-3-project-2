import React from 'react';

import './Header.scss';
import Logo from '../Logo';

function Header() {
  return (
    <header className="header">
      <Logo />
    </header>
  );
}

export default Header;
